# Implementações sugeridas:

1.Filtrar por Status: GET /api/fake_tasks?completed=true

2. Busca por Título: GET /api/fake_tasks?search=Javascript

3. Filtrar por Prioridade: GET /api/fake_tasks?priority=high

4. Obter a contagem total de tarefas, quantas estão concluídas e quantas são de alta prioridade: GET /api/fake_tasks/stats:

5. Criar a rota Arquivar como uma alternativa à opção de remoção. Como isso poderia ser feito ?

6. Incluir o campo UpdatedAt, para atualizar a data/hora sempre que a tarefa for atualizada;
